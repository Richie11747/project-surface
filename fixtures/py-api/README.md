# inventory-api

A deliberately small Python service used as a project-surface fixture. It has a
route decorator, a documented contract, tests that import the implementation,
and one required environment variable that is missing from `.env.example`.
