{
  "name": "checkout-api",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny zero-dependency API used as a project-surface fixture.",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "dev": "node src/server.ts"
  }
}
