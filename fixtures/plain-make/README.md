# plain-make

A project no language adapter recognises. It exists to show the fallback: the Makefile
targets and the `.env.example` names are read, nothing is guessed about behaviour, and
the document says so instead of coming back empty.
